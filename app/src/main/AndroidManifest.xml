<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.repaso1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ViewBActivity"/>
        <activity android:name=".Activities.CheckBoxActivity" />
        <activity android:name=".Activities.FragmentsActivity" />
        <activity android:name=".Activities.ConsultarUsuarioActivity" />
        <activity android:name=".Activities.RegistrarUsuarioSQLitleActivity" />
        <activity android:name=".Activities.SQLiteActivity" />
        <activity android:name=".Activities.sharedExampleActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>